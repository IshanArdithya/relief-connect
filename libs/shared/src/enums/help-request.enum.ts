/**
 * Enums for Help Request domain
 */
export enum HelpRequestCategory {
  FOOD_WATER = 'Food/Water',
  RESCUE = 'Rescue',
  MEDICAL = 'Medical',
  SHELTER = 'Shelter',
  OTHER = 'Other',
}

export enum Urgency {
  LOW = 'Low',
  MEDIUM = 'Medium',
  HIGH = 'High',
}

export enum ContactType {
  PHONE = 'Phone',
  WHATSAPP = 'WhatsApp',
  NONE = 'None',
}

export enum HelpRequestStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
  EXPIRED = 'EXPIRED',
}

